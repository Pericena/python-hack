## 📋 Endpoints disponibles en tu código

| Método   | Ruta                 | Descripción                                   |
| -------- | -------------------- | --------------------------------------------- |
| `GET`    | `/`                  | Muestra un mensaje de bienvenida.             |
| `GET`    | `/usuarios`          | Devuelve todos los usuarios (listado).        |
| `GET`    | `/usuarios/<int:id>` | Devuelve un usuario específico por su ID.     |
| `POST`   | `/usuarios`          | Crea un nuevo usuario con los datos enviados. |
| `DELETE` | `/usuarios/<int:id>` | Elimina un usuario por su ID.                 |
| `PUT`    | `/usuarios/<int:id>` | Modifica un usuario por su ID.                |

---

## 🧪 Ejemplos de uso con `curl`

### ➕ Crear usuario

```bash
curl -X POST http://localhost:5000/usuarios \
  -H "Content-Type: application/json" \
  -d '{"nombre": "Luishiño", "email": "luishiño@example.com"}'
```

---

### 📋 Ver todos los usuarios

```bash
curl http://localhost:5000/usuarios
```

---

### 🔍 Ver usuario por ID

```bash
curl http://localhost:5000/usuarios/1
```

---

### 🔁 Modificar usuario por ID

```bash
curl -X PUT http://localhost:5000/usuarios/1 \
  -H "Content-Type: application/json" \
  -d '{"nombre": "Luis Hacker"}'
```

---

### ❌ Eliminar usuario por ID

```bash
curl -X DELETE http://localhost:5000/usuarios/1
```

---
